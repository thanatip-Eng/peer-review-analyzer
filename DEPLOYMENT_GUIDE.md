# 📚 คู่มือ Deploy Peer Review Analyzer

## สารบัญ
1. [สร้าง GitHub Repository](#1-สร้าง-github-repository)
2. [อัปโหลดโค้ดผ่าน GitHub Web](#2-อัปโหลดโค้ดผ่าน-github-web)
3. [Deploy ขึ้น Vercel](#3-deploy-ขึ้น-vercel)
4. [ตั้งค่า Firebase (ถ้าต้องการ)](#4-ตั้งค่า-firebase-ถ้าต้องการ)

---

## 1. สร้าง GitHub Repository

### ขั้นตอนที่ 1.1: เข้า GitHub
1. ไปที่ [github.com](https://github.com)
2. Login เข้าสู่ระบบ (หรือสมัครใหม่ถ้ายังไม่มี)

### ขั้นตอนที่ 1.2: สร้าง Repository ใหม่
1. คลิกปุ่ม **"+"** มุมขวาบน → เลือก **"New repository"**

2. กรอกข้อมูล:
   ```
   Repository name: peer-review-analyzer
   Description: ระบบวิเคราะห์คะแนน Peer Review สำหรับ Canvas LMS
   ☑ Public (หรือ Private ถ้าต้องการ)
   ☐ Add a README file (ไม่ต้องติ๊ก)
   ```

3. คลิก **"Create repository"**

---

## 2. อัปโหลดโค้ดผ่าน GitHub Web

### ขั้นตอนที่ 2.1: เตรียมไฟล์
1. **แตก ZIP** ที่ดาวน์โหลดมา (peer-review-analyzer-v11.zip)
2. จะได้โฟลเดอร์ `peer-review-analyzer` ที่มีไฟล์ดังนี้:
   ```
   peer-review-analyzer/
   ├── index.html
   ├── package.json
   ├── vite.config.js
   ├── tailwind.config.js
   ├── postcss.config.js
   ├── src/
   │   ├── App.jsx
   │   ├── main.jsx
   │   ├── index.css
   │   └── utils/
   │       └── csvParser.js
   └── .gitignore
   ```

### ขั้นตอนที่ 2.2: อัปโหลดไฟล์ทั้งหมด

1. ในหน้า repository ที่เพิ่งสร้าง คลิก **"uploading an existing file"**
   
   หรือถ้าเห็นปุ่ม **"Add file"** → เลือก **"Upload files"**

2. **ลากไฟล์ทั้งหมด** จากในโฟลเดอร์ `peer-review-analyzer` ไปวางในช่อง upload
   
   ⚠️ **สำคัญ:** ลากไฟล์ที่อยู่ **ข้างใน** โฟลเดอร์ ไม่ใช่ตัวโฟลเดอร์เอง!

3. รอจนอัปโหลดครบทุกไฟล์

4. ที่ช่อง **"Commit changes"** ด้านล่าง:
   ```
   Add files via upload
   ```

5. คลิก **"Commit changes"**

### ขั้นตอนที่ 2.3: ตรวจสอบ
หลัง commit สำเร็จ ควรเห็นไฟล์ทั้งหมดใน repository:
```
📁 src/
📄 .gitignore
📄 index.html
📄 package.json
📄 postcss.config.js
📄 tailwind.config.js
📄 vite.config.js
```

---

## 3. Deploy ขึ้น Vercel

### ขั้นตอนที่ 3.1: สมัคร/Login Vercel
1. ไปที่ [vercel.com](https://vercel.com)
2. คลิก **"Sign Up"** หรือ **"Log In"**
3. เลือก **"Continue with GitHub"** (แนะนำ - เชื่อมต่อง่าย)
4. อนุญาตให้ Vercel เข้าถึง GitHub

### ขั้นตอนที่ 3.2: Import Project
1. ที่ Dashboard คลิก **"Add New..."** → **"Project"**

2. ในหน้า **"Import Git Repository"**:
   - จะเห็นรายการ repositories จาก GitHub
   - หา **"peer-review-analyzer"**
   - คลิก **"Import"**

3. ถ้าไม่เห็น repository:
   - คลิก **"Adjust GitHub App Permissions"**
   - เลือก repository ที่ต้องการ
   - คลิก **"Save"**

### ขั้นตอนที่ 3.3: Configure Project
1. **Framework Preset:** Vercel จะตรวจจับว่าเป็น **Vite** อัตโนมัติ ✓

2. **Build and Output Settings:** (ปกติไม่ต้องแก้)
   ```
   Build Command: npm run build
   Output Directory: dist
   Install Command: npm install
   ```

3. **Environment Variables:** (ยังไม่ต้องเพิ่มตอนนี้)

4. คลิก **"Deploy"**

### ขั้นตอนที่ 3.4: รอ Deploy
1. Vercel จะ:
   - Clone repository
   - Install dependencies
   - Build project
   - Deploy

2. ใช้เวลาประมาณ 1-2 นาที

3. เมื่อสำเร็จจะเห็น **"Congratulations!"** 🎉

### ขั้นตอนที่ 3.5: ได้ URL!
```
https://peer-review-analyzer.vercel.app
```

หรือ URL ที่ Vercel สร้างให้ เช่น:
```
https://peer-review-analyzer-xxxxx.vercel.app
```

### 🔄 การอัปเดตครั้งต่อไป
1. แก้ไขไฟล์ใน GitHub (หรืออัปโหลดใหม่)
2. Vercel จะ **auto deploy** ทันทีที่มีการเปลี่ยนแปลง!

---

## 4. ตั้งค่า Firebase (ถ้าต้องการ)

> 💡 **หมายเหตุ:** ระบบปัจจุบันทำงานได้โดยไม่ต้องมี database  
> Firebase จำเป็นเฉพาะถ้าต้องการ:
> - บันทึกประวัติการวิเคราะห์
> - แชร์ข้อมูลระหว่าง TA
> - เก็บ settings ของแต่ละวิชา

### ขั้นตอนที่ 4.1: สร้าง Firebase Project

1. ไปที่ [console.firebase.google.com](https://console.firebase.google.com)

2. Login ด้วย Google Account

3. คลิก **"Create a project"** (หรือ "Add project")

4. กรอกชื่อ Project:
   ```
   Project name: peer-review-system
   ```
   คลิก **Continue**

5. Google Analytics:
   - เลือก **Disable** ก็ได้ (ไม่จำเป็น)
   - คลิก **Create project**

6. รอสักครู่ → คลิก **Continue**

### ขั้นตอนที่ 4.2: เพิ่ม Web App

1. ในหน้า Project Overview คลิกไอคอน **</>** (Web)

2. กรอก App nickname:
   ```
   App nickname: peer-review-web
   ☐ Firebase Hosting (ไม่ต้องติ๊ก - เราใช้ Vercel)
   ```

3. คลิก **"Register app"**

4. จะได้ **Firebase Config** ที่ต้องเก็บไว้:
   ```javascript
   const firebaseConfig = {
     apiKey: "AIzaSy...",
     authDomain: "peer-review-system.firebaseapp.com",
     projectId: "peer-review-system",
     storageBucket: "peer-review-system.appspot.com",
     messagingSenderId: "123456789",
     appId: "1:123456789:web:abc123"
   };
   ```

5. คลิก **"Continue to console"**

### ขั้นตอนที่ 4.3: สร้าง Firestore Database

1. ในเมนูซ้าย คลิก **"Build"** → **"Firestore Database"**

2. คลิก **"Create database"**

3. เลือก Location:
   ```
   ☑ asia-southeast1 (Singapore) - ใกล้ไทยที่สุด
   ```

4. Security Rules:
   ```
   ☑ Start in test mode (เพื่อทดสอบก่อน)
   ```
   ⚠️ **จำกัดเวลา 30 วัน** - ต้องแก้ rules ก่อนหมดเวลา

5. คลิก **"Create"**

### ขั้นตอนที่ 4.4: เพิ่ม Firebase Config ใน Vercel

1. ไปที่ [vercel.com](https://vercel.com) → Project ของคุณ

2. คลิก **"Settings"** → **"Environment Variables"**

3. เพิ่ม Variables ทีละตัว:

   | Name | Value |
   |------|-------|
   | `VITE_FIREBASE_API_KEY` | AIzaSy... |
   | `VITE_FIREBASE_AUTH_DOMAIN` | peer-review-system.firebaseapp.com |
   | `VITE_FIREBASE_PROJECT_ID` | peer-review-system |
   | `VITE_FIREBASE_STORAGE_BUCKET` | peer-review-system.appspot.com |
   | `VITE_FIREBASE_MESSAGING_SENDER_ID` | 123456789 |
   | `VITE_FIREBASE_APP_ID` | 1:123456789:web:abc123 |

4. คลิก **"Save"** ทุกตัว

5. ไปที่ **"Deployments"** → คลิก **"Redeploy"** เพื่อใช้ค่าใหม่

### ขั้นตอนที่ 4.5: Security Rules (สำคัญ!)

เมื่อพร้อมใช้งานจริง ต้องแก้ Security Rules:

1. ไปที่ Firebase Console → Firestore → **Rules**

2. แก้เป็น:
   ```javascript
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       // อนุญาตเฉพาะ authenticated users
       match /{document=**} {
         allow read, write: if request.auth != null;
       }
     }
   }
   ```

3. คลิก **"Publish"**

---

## 📋 สรุป Checklist

### Deploy พื้นฐาน (ไม่ใช้ Firebase)
- [ ] สร้าง GitHub repository
- [ ] อัปโหลดไฟล์ทั้งหมด
- [ ] เชื่อมต่อ Vercel กับ GitHub
- [ ] Deploy สำเร็จ
- [ ] ทดสอบใช้งาน

### เพิ่ม Firebase (ถ้าต้องการ)
- [ ] สร้าง Firebase project
- [ ] สร้าง Firestore database
- [ ] เพิ่ม Environment Variables ใน Vercel
- [ ] Redeploy
- [ ] แก้ Security Rules

---

## ❓ FAQ

**Q: ต้องจ่ายเงินไหม?**
> ไม่ต้อง! ทั้ง GitHub, Vercel (Free tier), และ Firebase (Spark plan) ฟรีทั้งหมด

**Q: ข้อมูลถูกส่งไปเก็บที่ไหน?**
> ถ้าไม่ใช้ Firebase → ข้อมูลประมวลผลใน browser ของผู้ใช้เท่านั้น ไม่ส่งไปไหน

**Q: รองรับผู้ใช้พร้อมกันกี่คน?**
> Vercel Free tier รองรับได้เยอะมาก (100GB bandwidth/เดือน)

**Q: แก้โค้ดยังไงหลัง deploy แล้ว?**
> แก้ไฟล์ใน GitHub → Vercel auto deploy ภายใน 1-2 นาที

---

## 🆘 ติดปัญหา?

1. **Build failed:** ตรวจสอบว่าอัปโหลดไฟล์ครบ โดยเฉพาะ `package.json`
2. **หน้าเว็บว่างเปล่า:** ตรวจสอบ Console ใน Developer Tools (F12)
3. **Firebase ไม่ทำงาน:** ตรวจสอบ Environment Variables ใน Vercel

---

*สร้างโดย Peer Review Analyzer v11*
